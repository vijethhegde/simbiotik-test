import { Component } from '@angular/core';
@Component({
  selector: 'app-sidebar',
  imports: [],
  templateUrl: './sidebar.html',
  styleUrl: './sidebar.scss',
  standalone: true,
})
export class Sidebar {
  // Add component logic here (e.g., logout method)
  logout() {
    // implement logout logic or emit event
  }
}

